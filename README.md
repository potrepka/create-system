# create-system

Without installation:

```bash
bun create system
```

With global installation:

```bash
bun add -g create-system

# Usage
create-system
```

For local development:

```bash
bun install
bun run build

# Usage
bun bin/index.js
```
